@model IEnumerable<Event_time_Project.Models.Notification>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}




<br />
<br />

    <h2>Filterable Table</h2>
    <p>Type something in the input field to search the table for first names, last names or emails:</p>
    <input id="myInput" type="text" placeholder="Search..">
    <br><br>

    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table>
        <thead>
            <tr>
                <th>Notify Date:</th>
                <th>Operations</th>
            </tr>
        </thead>
        <tbody id="myTable">


            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Notify_date)
                    </td>
                    <td>
                        @Html.ActionLink("Edit", "Edit", new { id = item.Notify_Id }) |
                        @Html.ActionLink("Details", "Details", new { id = item.Notify_Id }) |
                        @Html.ActionLink("Delete", "Delete", new { id = item.Notify_Id })
                    </td>
                </tr>
            }
        </tbody>
    </table>
